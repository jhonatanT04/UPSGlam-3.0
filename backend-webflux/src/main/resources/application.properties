spring.application.name=upsglam-backend
server.port=${SERVER_PORT}
server.error.include-message=always

supabase.url=${SUPABASE_URL}
supabase.key=${SUPABASE_KEY}
gpu.service.url=${GPU_SERVICE_URL:http://localhost:8000}